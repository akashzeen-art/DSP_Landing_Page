package com.zeen.lpstudio.domain;

public enum UiTemplate {
    /** Mobio-style Google Palestine UI (operator + PIN + Google tags) */
    GOOGLE_MOBILE,
    /** movstreamlite Propeller UI (mobile → PIN, optional operator) */
    PROPELLER_MOVSTREAM
}
